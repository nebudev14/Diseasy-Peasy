datasource db {
    provider = "postgresql"
    url      = "postgresql://postgres:YunAwesome123@localhost/diseases"
}

generator db {
    provider = "go run github.com/prisma/prisma-client-go"
}

model Disease {
    name        String  @id
    part        String
    symptoms    String[]
}