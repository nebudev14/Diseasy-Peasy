datasource db {
    provider = "postgresql"
    url      = env("DATABASE_URL")
}

generator db {
    provider = "go run github.com/prisma/prisma-client-go"
}

model Disease {
    name        String  @id
    body_part   String
    symptoms    String[]
}

